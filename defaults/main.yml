---
    mail_smarthost: "localhost"

    admin_mail: ""

    install_prefix: "/opt/SI5000"

    software_version: "v0.16.0"
    deployment_archive_url: "https://github.com/prometheus/node_exporter/releases/download/{{ software_version }}/node_exporter-{{ software_version }}.linux-amd64.tar.gz"
