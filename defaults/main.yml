---
    mail_smarthost: "localhost"

    admin_mail: ""

    docker_network_name: monitoring-net

    install_prefix: "/opt/SI5000"

    blackbox_port: 9115

    grafana_user: grafana
    grafana_password: grafana

    software_version: "0.16.0"
    deployment_archive_url: "https://github.com/prometheus/node_exporter/releases/download/v{{ software_version }}/node_exporter-{{ software_version }}.linux-amd64.tar.gz"

    software_version_bl: "0.13.0"
    deployment_archive_url_bl: "https://github.com/prometheus/blackbox_exporter/releases/download/v{{ software_version_bl }}/blackbox_exporter-{{ software_version_bl }}.linux-amd64.tar.gz"
